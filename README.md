# Snorgnote

Локальное open-source desktop-приложение (Tauri v2 + Rust + React/TS), которое объединяет vault заметок, pipeline обработки входящих данных, навыки-автоматизации (skills), планирование, фокус и метрики прогресса.

## Технологии

- `Tauri v2`
- `Rust core`
- `SQLite + FTS5`
- `React + TypeScript + Vite`
- `Tailwind CSS + lucide-react`

## Статус

Текущая версия: `v0.1.12`.

Реализовано:

- локальный `vault` с Markdown-заметками и индексом;
- `inbox -> jobs -> vault/tasks` конвейер;
- очередь jobs с retry и событиями;
- YAML `skills` (валидация, запуск, встроенные skills);
- генерация `Daily` и `Weekly` заметок;
- `spaced review` базовый daily-блок;
- `focus/pomodoro` старт/стоп и статистика;
- `projects` состояние (health/xp/level);
- dashboard-метрики и функциональные UI-панели.

## Быстрый запуск

```bash
npm install
npm run tauri:dev
```

Web mock режим (без Tauri runtime):

```bash
npm run dev
```

## Сводка версий (RU)

### v0.1.0

- Создана архитектурная основа приложения с Rust core и Tauri commands.
- Поднята схема SQLite/FTS5 и миграция `001_init.sql`.
- Добавлены базовые модули: vault, inbox, jobs, skills, planner, focus, dashboard.
- Добавлен UI-каркас с панелями: Inbox, Notes, Daily/Weekly, Projects, Focus, Stats.
- Добавлены начальные встроенные skills: `daily_planner`, `spaced_review`, `mood_money_events_summary`.
- Подготовлены docs и roadmap для дальнейшего расширения.

### v0.1.1

- Добавлена рабочая Telegram-интеграция для сценария `1 user + 1 bot` по `username`.
- Реализованы настройки Telegram в UI: `bot token`, `username`, генерация one-time кода, `Poll Now`, `Start/Stop listener`.
- Реализована верификация через код из лички боту и фильтрация только личных сообщений нужного username.
- Добавлен long polling listener в Rust core и запись принятых сообщений из Telegram в `Inbox`.
- Добавлена миграция `002_telegram.sql` и новые типы/команды для Telegram статуса и управления.

### v0.1.2

- Исправлено сохранение Telegram-конфига: теперь `Save Config` не падает из-за сетевой проверки `getMe`.
- Улучшено отображение ошибок Tauri invoke в UI: показывается конкретный текст ошибки вместо общего `failed to save config`.
- Сценарий стал устойчивее для нестабильной сети/блокировок API: конфиг сохраняется локально, проверка фактически происходит на шаге polling/verification.

### v0.1.3

- Исправлен маппинг аргументов Tauri invoke на camelCase для команд с параметрами.
- Устранена ошибка `missing required key botToken` при `telegram_set_config`.
- Заодно выровнены вызовы `vault_save_note`, `inbox_add_item`, `focus_start` под корректные ключи аргументов.

### v0.1.4

- Интерфейс клиента локализован на русский: вкладки, заголовки, кнопки, плейсхолдеры, статусы и ошибки.
- Локализованы user-facing fallback тексты в `src/lib/api.ts` для web mock режима и сообщений ошибок.
- Во входящих добавлены русские подписи источников/статусов элементов (`quick_note`, `new`, `processed`).

### v0.1.5

- Добавлена кнопка смены языка в `Настройки` (RU/EN) с мгновенным применением и сохранением выбора в `localStorage`.
- Добавлен `LocaleProvider` и базовая i18n-инфраструктура (`src/lib/locale.tsx`) для всего React-клиента.
- Основные UI-панели переключаются между русским и английским текстом без перезапуска приложения.

### v0.1.6

- Добавлена корзина для заметок: удаление заметки в корзину и восстановление из UI.
- Добавлена корзина для входящих: удаление элемента входящих и восстановление в исходный статус.
- Ускорена загрузка заметок: `vault` теперь индексируется инкрементально (без полного перечитывания всех markdown-файлов на каждый заход).

### v0.1.7

- Добавлено окончательное удаление (`hard delete`) для заметок и входящих из корзины, а также кнопки полной очистки корзин.
- Ускорен рендер больших списков в UI через windowed rendering (`VirtualList`) для заметок, входящих и корзин.
- Добавлен дебаунс переиндексации vault в Rust core, чтобы частые открытия списков не вызывали лишние полные проходы по файлам.

### v0.1.8

- Добавлен кастомный заголовок окна (frameless) с drag-зоной и кнопками `свернуть / развернуть / закрыть`.
- Добавлена система тем `system / light / dark` с переключателем в `Настройки` и мгновенным применением.
- Заголовок окна синхронизирован с режимом темы (`Snorgnote · Системная/Светлая/Тёмная`).
- Добавлен unit-тест для утилит темы (`vitest`): нормализация режима, резолв system, локализация лейблов и заголовка.

### v0.1.9

- Исправлена работа кнопок кастомного titlebar (`свернуть / развернуть / закрыть`) в Tauri v2 через явные window-permissions в `capabilities/default.json`.
- Добавлена безопасная обработка ошибок window API в `window-titlebar.tsx`, чтобы отказ прав не приводил к "тихим" кликам без диагностики.

### v0.1.10

- Исправлено перетаскивание frameless-окна: добавлен явный вызов `startDragging()` по левому клику в drag-области titlebar.
- Добавлено необходимое Tauri permission `core:window:allow-start-dragging` в `capabilities/default.json`.
- Добавлен unit-тест для условия старта drag (`window-titlebar-utils.test.ts`).

### v0.1.12

- Убрана подпись текущей темы из текста titlebar: теперь в заголовке отображается только `Snorgnote`.
- Сохранены рабочие кнопки окна и drag-перетаскивание frameless-окна.


- Добавлена 4-я тема `Кастомная` в настройках рядом с `system/light/dark`.
- Для кастомной темы добавлен выбор `основного` и `дополнительного` цвета из палитры.
- Выбранные цвета сохраняются локально и применяются через CSS-переменные (`--primary`, `--surface-gradient`, `--bg-radial-*`).
- Цвета нормализуются по разрешенной палитре, чтобы исключить некорректные значения в конфиге.
