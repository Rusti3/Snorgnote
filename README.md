# Snorgnote

Локальное open-source desktop-приложение (Tauri v2 + Rust + React/TS), которое объединяет vault заметок, pipeline обработки входящих данных, навыки-автоматизации (skills), планирование, фокус и метрики прогресса.

## Технологии

- `Tauri v2`
- `Rust core`
- `SQLite + FTS5`
- `React + TypeScript + Vite`
- `Tailwind CSS + lucide-react`

## Статус

Текущая версия: `v0.1.24`.

Реализовано:

- локальный `vault` с Markdown-заметками и индексом;
- `inbox -> jobs -> vault/tasks` конвейер;
- очередь jobs с retry и событиями;
- YAML `skills` (валидация, запуск, встроенные skills);
- генерация `Daily` и `Weekly` заметок;
- `spaced review` базовый daily-блок;
- `focus/pomodoro` старт/стоп и статистика;
- `projects` состояние (health/xp/level);
- dashboard-метрики и функциональные UI-панели.

## Быстрый запуск

```bash
npm install
npm run tauri:dev
```

Web mock режим (без Tauri runtime):

```bash
npm run dev
```

## Сводка версий (RU)

### v0.1.0

- Создана архитектурная основа приложения с Rust core и Tauri commands.
- Поднята схема SQLite/FTS5 и миграция `001_init.sql`.
- Добавлены базовые модули: vault, inbox, jobs, skills, planner, focus, dashboard.
- Добавлен UI-каркас с панелями: Inbox, Notes, Daily/Weekly, Projects, Focus, Stats.
- Добавлены начальные встроенные skills: `daily_planner`, `spaced_review`, `mood_money_events_summary`.
- Подготовлены docs и roadmap для дальнейшего расширения.

### v0.1.1

- Добавлена рабочая Telegram-интеграция для сценария `1 user + 1 bot` по `username`.
- Реализованы настройки Telegram в UI: `bot token`, `username`, генерация one-time кода, `Poll Now`, `Start/Stop listener`.
- Реализована верификация через код из лички боту и фильтрация только личных сообщений нужного username.
- Добавлен long polling listener в Rust core и запись принятых сообщений из Telegram в `Inbox`.
- Добавлена миграция `002_telegram.sql` и новые типы/команды для Telegram статуса и управления.

### v0.1.2

- Исправлено сохранение Telegram-конфига: теперь `Save Config` не падает из-за сетевой проверки `getMe`.
- Улучшено отображение ошибок Tauri invoke в UI: показывается конкретный текст ошибки вместо общего `failed to save config`.
- Сценарий стал устойчивее для нестабильной сети/блокировок API: конфиг сохраняется локально, проверка фактически происходит на шаге polling/verification.

### v0.1.3

- Исправлен маппинг аргументов Tauri invoke на camelCase для команд с параметрами.
- Устранена ошибка `missing required key botToken` при `telegram_set_config`.
- Заодно выровнены вызовы `vault_save_note`, `inbox_add_item`, `focus_start` под корректные ключи аргументов.

### v0.1.4

- Интерфейс клиента локализован на русский: вкладки, заголовки, кнопки, плейсхолдеры, статусы и ошибки.
- Локализованы user-facing fallback тексты в `src/lib/api.ts` для web mock режима и сообщений ошибок.
- Во входящих добавлены русские подписи источников/статусов элементов (`quick_note`, `new`, `processed`).

### v0.1.5

- Добавлена кнопка смены языка в `Настройки` (RU/EN) с мгновенным применением и сохранением выбора в `localStorage`.
- Добавлен `LocaleProvider` и базовая i18n-инфраструктура (`src/lib/locale.tsx`) для всего React-клиента.
- Основные UI-панели переключаются между русским и английским текстом без перезапуска приложения.

### v0.1.6

- Добавлена корзина для заметок: удаление заметки в корзину и восстановление из UI.
- Добавлена корзина для входящих: удаление элемента входящих и восстановление в исходный статус.
- Ускорена загрузка заметок: `vault` теперь индексируется инкрементально (без полного перечитывания всех markdown-файлов на каждый заход).

### v0.1.7

- Добавлено окончательное удаление (`hard delete`) для заметок и входящих из корзины, а также кнопки полной очистки корзин.
- Ускорен рендер больших списков в UI через windowed rendering (`VirtualList`) для заметок, входящих и корзин.
- Добавлен дебаунс переиндексации vault в Rust core, чтобы частые открытия списков не вызывали лишние полные проходы по файлам.

### v0.1.8

- Добавлен кастомный заголовок окна (frameless) с drag-зоной и кнопками `свернуть / развернуть / закрыть`.
- Добавлена система тем `system / light / dark` с переключателем в `Настройки` и мгновенным применением.
- Заголовок окна синхронизирован с режимом темы (`Snorgnote · Системная/Светлая/Тёмная`).
- Добавлен unit-тест для утилит темы (`vitest`): нормализация режима, резолв system, локализация лейблов и заголовка.

### v0.1.9

- Исправлена работа кнопок кастомного titlebar (`свернуть / развернуть / закрыть`) в Tauri v2 через явные window-permissions в `capabilities/default.json`.
- Добавлена безопасная обработка ошибок window API в `window-titlebar.tsx`, чтобы отказ прав не приводил к "тихим" кликам без диагностики.

### v0.1.10

- Исправлено перетаскивание frameless-окна: добавлен явный вызов `startDragging()` по левому клику в drag-области titlebar.
- Добавлено необходимое Tauri permission `core:window:allow-start-dragging` в `capabilities/default.json`.
- Добавлен unit-тест для условия старта drag (`window-titlebar-utils.test.ts`).

### v0.1.12

- Убрана подпись текущей темы из текста titlebar: теперь в заголовке отображается только `Snorgnote`.
- Сохранены рабочие кнопки окна и drag-перетаскивание frameless-окна.
- Добавлена 4-я тема `Кастомная` в настройках рядом с `system/light/dark`.
- Для кастомной темы добавлен выбор `основного` и `дополнительного` цвета из палитры.
- Выбранные цвета сохраняются локально и применяются через CSS-переменные.
- Цвета нормализуются по разрешенной палитре, чтобы исключить некорректные значения в конфиге.

### v0.1.13

- Добавлен `live-preview` кастомной темы в настройках: можно видеть результат выбора цветов до применения.
- Перераспределены роли цветов кастомной темы:
- `основной` цвет теперь отвечает за `background`, `card`, `titlebar` и общую атмосферу интерфейса.
- `дополнительный` цвет теперь отвечает за `buttons` и акцентные состояния (`--primary`).
- Обновлен titlebar: используется отдельная переменная `--titlebar-bg`, связанная с основным цветом темы.
- Добавлены/обновлены unit-тесты для нового контракта `buildCustomThemeVars`.


### v0.1.14

- Убран live-preview кастомной темы в настройках.
- Исправлена drag-зона titlebar: теперь окно перетаскивается по всей панели, кроме кнопок управления.
- Добавлен unit-тест для фильтрации кликов по кнопкам окна (window-titlebar-utils.test.ts).


### v0.1.15

- Кнопки titlebar (minimize/maximize/close) теперь кликаются по всей высоте панели (от верхнего до нижнего края).
- Убраны мертвые зоны по Y вокруг кнопок окна.
- Drag окна сохранен на остальной области titlebar.

### v0.1.16

- Убрана отдельная тема `Кастомная`: теперь кастомизация встроена в режимы `Светлая` и `Тёмная`.
- Для каждой темы хранятся свои цвета `primary/secondary`: `primary` отвечает за фон + titlebar, `secondary` за кнопки и акценты.
- В `System` режиме палитра не редактируется; добавлена подсказка настраивать цвета отдельно в `Светлая`/`Тёмная`.
- Добавлены новые ключи локального хранения (`snorgnote.light_*`, `snorgnote.dark_*`) и очищаются legacy-ключи `snorgnote.custom_*`; legacy-режим `custom` автоматически переводится в `light`.
- Обновлены unit-тесты `theme-utils`: удалён `custom` режим, добавлены проверки для light/dark-палитр и fallback-логики.

### v0.1.17

- Для светлой темы в палитру основного цвета добавлен `#ffffff` (белый).
- Для тёмной темы в палитру основного цвета добавлен `#000000` (чёрный).
- Основные палитры разделены по режимам: выбор и валидация light/dark теперь используют разные списки допустимых цветов.
- Обновлены unit-тесты `theme-utils` для проверки mode-specific палитр основного цвета.

### v0.1.18

- Добавлен глобальный кастомный scrollbar для приложения.
- Цвет ползунка (`thumb`) теперь берется из `--foreground`.
- Цвет дорожки (`track`) теперь берется из `--background`.
- Стили работают в WebKit-браузерах (`::-webkit-scrollbar`) и Firefox (`scrollbar-color` / `scrollbar-width`).

### v0.1.19

- Добавлена интеграция браузерного расширения через deep-link `snorgnote://new?data=...`.
- В Rust core добавлен парсер/валидация payload клипера (base64url + JSON), нормализация источника и формирование контента для Inbox.
- Клип из браузера теперь сохраняется во входящие с источником `browser` (в UI отображается как браузер), с тегами `browser`, `web-clipper` и типом клипа.
- В `raw_payload_json` сохраняется исходный payload deep-link для трассировки и отладки.
- Добавлена авто-регистрация протокола `snorgnote://` на Windows (HKCU `Software\\Classes\\snorgnote`).
- Подключен single-instance обработчик: если приложение уже запущено, новый deep-link аргумент тоже будет принят и добавлен во входящие без принудительного фокуса окна.
- Добавлены unit-тесты для deep-link парсинга, ingestion в `AppState` и сохранения `raw_payload_json`.

### v0.1.21

- Полностью переработана вкладка `Focus`: добавлен новый Pomodoro-таймер в стиле винтажных часов (по дизайну из `watchanc.js`) с адаптацией под светлую/тёмную тему.
- Добавлены полноценные команды фокуса в backend: `focus_active`, `focus_pause`, `focus_resume`.
- Добавлена поддержка пауз в БД (`paused_at`, `paused_total_sec`) и корректный расчёт `duration_sec` без времени на паузе.
- Логика Pomodoro переведена на кастомный конструктор: `focus`, `short break`, `long break`, `long every N cycles`; переходы между фазами ручные (без автозапуска).
- Добавлено восстановление состояния таймера после перезапуска в режиме «всегда на паузе».
- В статистику фокуса учитываются только focus-фазы (перерывы не записываются как продуктивное время).
- Добавлены тесты:
- Frontend: `pomodoro-engine`, `pomodoro-storage`, `api-focus`.
- Backend: сценарии `focus_pause/resume/active` в `core::focus::tests`.
### v0.1.20

- Добавлено авто-обновление вкладки Inbox по событию `snorgnote://inbox-updated`: новый browser clip появляется без ручного `Refresh`.
- В Rust runtime после успешного deep-link ingest отправляется Tauri event в UI с `item_id` и `source`.
- Добавлена дедупликация browser clip по `normalized_url + content_sha256`:
- повторный одинаковый clip не создает второй элемент во входящих;
- clip с тем же URL, но другим контентом добавляется как новый.
- В `raw_payload_json` browser clip теперь сохраняется блок `dedup` (`key`, `normalized_url`, `content_sha256`) для диагностики и контроля.
- Добавлены тесты:
- Rust: стабильность dedup-key, нормализация URL, дедуп/не-дедуп сценарии ingestion;
- Frontend: подписка на событие inbox update и корректный callback.

### v0.1.22

- Добавлена история фокус-сессий с пагинацией:
- backend-команда `focus_history` (Rust + Tauri command) с фильтрами и `total/limit/offset`;
- mock-реализация в `api.focusHistory(...)`;
- отдельный блок `Focus history` во вкладке Focus с листингом завершенных сессий и кнопками `Prev/Next`.
- Добавлены системные уведомления и короткий встроенный звук при автоматическом завершении фазы Pomodoro.
- Добавлены типы API для истории фокуса (`FocusHistoryItem`, `FocusHistoryPage`) в Rust и TypeScript.
- Добавлены тесты:
- Frontend: `notifications.test.ts`, проверка `api.focusHistory` в `api-focus.test.ts`;
- Backend: `focus_history_returns_completed_sessions_only`, `focus_history_supports_project_filter_and_pagination`.

### v0.1.23

- Добавлена новая вкладка `Flashcards` с anki-подобным базовым режимом повторения:
- показ front/back;
- оценки `Again / Hard / Good / Easy`;
- пересчет `due_at`, `interval_days`, `ease_factor`, `reps`, `lapses`.
- Добавлена гибридная модель хранения карточек:
- SQLite таблицы `flashcards` и `flashcard_reviews` (миграция `005_flashcards.sql`);
- markdown mirror для каждой карточки в `vault/Flashcards/<id>.md`.
- Добавлены backend-команды flashcards (create/list/get/update/review) через Tauri.
- В `Notes` добавлен множественный выбор и массовая отправка выбранных заметок в карточки (`1 заметка = 1 карточка`, с обязательной связью на source note).
- Добавлены новые типы и API-методы flashcards в frontend (`src/types/api.ts`, `src/lib/api.ts`) + mock-реализация.
- Добавлены тесты:
- Frontend: `api-flashcards.test.ts`;
- Backend: `core::flashcards::tests` (создание, импорт из заметок, дедуп, review-алгоритм, due-очередь).

### v0.1.24

- Обновлена вкладка `Projects`:
- каждый проект теперь можно раскрыть;
- внутри раскрытия показываются карточки заметок проекта с превью;
- добавлен modal-просмотр полного содержимого заметки прямо из проекта.
- Добавлен полный CRUD задач проекта:
- создание задачи с полями `title/status/energy/due`;
- редактирование задачи на месте;
- удаление задачи из проекта.
- Во вкладке `Notes` добавлено массовое назначение выбранных заметок в выбранный проект.
- Добавлены новые backend-команды и типы API:
- `projects_list_details`, `projects_assign_notes`, `projects_task_create`, `projects_task_update`, `projects_task_delete`.
- Добавлена миграция `006_projects_perf.sql` с индексами для ускорения выборок заметок/задач по проектам.
- Добавлены тесты:
- Frontend: `api-projects.test.ts`;
- Backend: новые тесты в `core::focus::tests` для assign/details/task CRUD/валидации.

