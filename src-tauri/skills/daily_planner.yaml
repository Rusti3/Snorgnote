id: daily_planner
version: 1
enabled: true
inputs:
  sources:
    - tasks
    - reviews
    - notes
jobs:
  - type: plan_daily
outputs:
  - target: vault_note
    path_template: "Daily/{{date}}.md"
triggers:
  - event: app.startup
schedule:
  cron: "0 7 * * *"
